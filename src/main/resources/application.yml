server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/facebook
    username: root
    password: root  
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: false
    database-platform: org.hibernate.dialect.MySQLDialect
   
   